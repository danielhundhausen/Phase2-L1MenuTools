class Object:

    def __init__(self, name):
        self.name = name
        # TODO: Load all yamls that define particles and populate the fileds of
        # this object with the one where `name` matches.
